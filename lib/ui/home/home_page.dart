import 'package:flutter/material.dart';
import 'view/home_form.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) => const HomeScreen();
}
